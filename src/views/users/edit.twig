{% extends 'base.twig' %}

{% block title %}
    {{ 'Edit user'|trans }}
{% endblock %}

{% block content %}
    <form action="{{ url('users_edit', {id: id}) }}" method="post">
        {{ form_widget(form) }}
        <input type="submit" name="submit" value="{{ 'Save'|trans }}"/>
    </form>
{% endblock %}