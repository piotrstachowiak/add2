{% extends 'base.twig' %}

{% block title %}
    {{ 'Change password'|trans }}
{% endblock %}

{% block content %}
    <form action="{{ url('profile_change_password') }}" method="post">
        {{ form_widget(form) }}
        <input type="submit" name="submit" value="{{ 'Save'|trans }}"/>
    </form>
{% endblock %}